<%= form_for(@museum_user) do |f| %>
  <% if @museum_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@museum_user.errors.count, "error") %> prohibited this museum_user from being saved:</h2>

      <ul>
      <% @museum_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
