<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @room.name %>
</p>
<%= link_to 'Show', museum_exhibition_room_artworks_path(@exhibition, @museum, @room) %> |
<%= link_to 'Edit', edit_museum_exhibition_room_path(@museum, @exhibition, @room) %> |
<%= link_to 'Back', museum_exhibition_rooms_path %>
