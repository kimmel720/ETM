<%= form_for(@panorama) do |f| %>
  <% if @panorama.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@panorama.errors.count, "error") %> prohibited this panorama from being saved:</h2>

      <ul>
      <% @panorama.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :show %><br>
    <%= f.text_field :show %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
